import { LiquefactionOverlaysPipe } from './liquefaction-overlays.pipe';

describe('LiquefactionOverlaysPipe', () => {
  it('create an instance', () => {
    const pipe = new LiquefactionOverlaysPipe();
    expect(pipe).toBeTruthy();
  });
});
