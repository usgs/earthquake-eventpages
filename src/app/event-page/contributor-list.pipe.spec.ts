import { ContributorListPipe } from './contributor-list.pipe';

describe('ContributorListPipe', () => {
  it('create an instance', () => {
    const pipe = new ContributorListPipe();
    expect(pipe).toBeTruthy();
  });
});
