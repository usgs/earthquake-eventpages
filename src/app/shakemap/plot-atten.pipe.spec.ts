import { PlotAttenPipe } from './plot-atten.pipe';

describe('PlotAttenPipe', () => {
  it('create an instance', () => {
    const pipe = new PlotAttenPipe();
    expect(pipe).toBeTruthy();
  });
});
