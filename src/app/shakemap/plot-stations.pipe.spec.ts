import { PlotStationsPipe } from './plot-stations.pipe';

describe('PlotStationsPipe', () => {
  it('create an instance', () => {
    const pipe = new PlotStationsPipe();
    expect(pipe).toBeTruthy();
  });
});
